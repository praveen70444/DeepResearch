services:
  - type: web
    name: deep-researcher
    env: python
    plan: free
    buildCommand: pip install -r requirements-lite.txt
    startCommand: python api_server_lite.py
    envVars:
      - key: PYTHON_VERSION
        value: 3.9.18
